spring.application.name=ShadowVibe
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:60811}/${DB_NAME:eclipsedb}
spring.datasource.username=${DB_USER:myeclipse}
spring.datasource.password=${DB_PASSWORD:secreteclipse}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
server.port=${PORT:1010}

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
app.upload.dir=uploads

app.admin.username=admin
app.admin.password=admin123
app.admin.email=admin@example.com